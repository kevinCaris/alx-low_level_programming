#include <stdio.h>
#include "main.h"
/**
 * _puts - prints a string, followed by a new line, to stdout.
 *@str:The characters to be checked.
 *
 *
 * Return: str.
 */


void _puts(char *str)
{
puts(str);
}
